# Ignore all contents inside jobs/
jobs/*
# But do not ignore the jobs/ folder itself
!jobs/

# Ignore all contents inside Archive/
Archive/*
# But do not ignore the Archive/ folder itself
!Archive/

# Ignore Copilot instructions file
.github/copilot-instructions.md
